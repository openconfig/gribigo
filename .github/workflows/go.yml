name: Go

on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  common:
    uses: openconfig/common-ci/.github/workflows/basic_go.yml@8d2987b8a26f4f3190a7f2e7db60f18489a3624a
    with:
      race-tests-excludes-regex: compliance
