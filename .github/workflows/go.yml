name: Go

on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  common:
    uses: openconfig/common-ci/.github/workflows/basic_go.yml@156c798bb9222f94d826b4f8540437ccc275e071
    with:
      race-tests-excludes-regex: compliance
